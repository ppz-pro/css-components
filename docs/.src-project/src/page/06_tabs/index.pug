extends ../page-layout
include ../mixins

block page
  h2 CSS #{pageName}

  h3 基本用法
  ul
    li
      | 引入 css:
      code.inline= '<link rel="stylesheet" href=".../tabs.css">'
    li
      | 一些 html:
      code.inline= config.code.basic
  
  p 最简案例：
  +demo(demos[''])
