extends ../../demo-layout.pug

block head
  title CCS .switch
  link(rel='stylesheet' href=references.switch)
  style.
    .ppz-ccs.switch {
      --font-size: 100px; /* 默认为 16px，如果你想要 2 倍大，则需设置为 32px */
      --ani-duration: 2s; /* 所有动画在 2 秒内完成 */
    }
block body
  input.ppz-ccs.switch(type='checkbox')
